# Руководство по внесению вклада

Спасибо за интерес к улучшению OBS Timer Plugin! 🎉

## Как помочь проекту

### 🐛 Сообщить о баге

1. Проверьте, нет ли уже [похожего issue](https://github.com/yourusername/obs-timer-plugin/issues)
2. Используйте шаблон issue для багов
3. Включите:
   - Версию OBS Studio
   - Версию плагина
   - Операционную систему
   - Шаги для воспроизведения
   - Ожидаемое и фактическое поведение
   - Скриншоты (если применимо)
   - Лог-файлы OBS

### 💡 Предложить улучшение

1. Проверьте [TODO.md](TODO.md) - может быть, идея уже есть
2. Создайте issue с тегом "enhancement"
3. Опишите:
   - Зачем нужна функция
   - Как она должна работать
   - Альтернативные варианты

### 📝 Улучшить документацию

- Исправить опечатки
- Дополнить примеры
- Перевести на другие языки
- Добавить скриншоты/видео

### 💻 Написать код

## Процесс разработки

### 1. Подготовка

```bash
# Форкните репозиторий на GitHub
# Клонируйте свой форк
git clone https://github.com/yourusername/obs-timer-plugin.git
cd obs-timer-plugin

# Добавьте основной репозиторий как upstream
git remote add upstream https://github.com/originalauthor/obs-timer-plugin.git
```

### 2. Создание ветки

```bash
# Обновите master
git checkout master
git pull upstream master

# Создайте ветку для вашей функции
git checkout -b feature/amazing-feature
# или для исправления бага
git checkout -b fix/bug-description
```

### 3. Разработка

```bash
# Настройте окружение
mkdir build && cd build
cmake .. -DCMAKE_PREFIX_PATH="путь/к/obs"

# Соберите проект
cmake --build . --config Release

# Протестируйте изменения в OBS
```

### 4. Коммиты

Используйте понятные сообщения коммитов:

```bash
# Хорошие примеры:
git commit -m "Add fade effect configuration option"
git commit -m "Fix timer reset not working on Windows"
git commit -m "Update Russian translation"

# Плохие примеры:
git commit -m "fix"
git commit -m "update"
git commit -m "stuff"
```

#### Формат сообщений коммитов

```
<type>: <subject>

<body>

<footer>
```

**Типы:**
- `feat`: Новая функция
- `fix`: Исправление бага
- `docs`: Изменения в документации
- `style`: Форматирование кода
- `refactor`: Рефакторинг без изменения функциональности
- `test`: Добавление тестов
- `chore`: Обслуживание проекта

**Примеры:**
```
feat: Add milliseconds display option

Adds a new property to display timer with milliseconds precision.
Users can now choose between MM:SS and MM:SS.mmm formats.

Closes #42
```

### 5. Тестирование

Перед отправкой PR убедитесь:

- [ ] Код компилируется без ошибок и предупреждений
- [ ] Плагин загружается в OBS Studio
- [ ] Новая функциональность работает как ожидается
- [ ] Не сломана существующая функциональность
- [ ] Код соответствует стилю проекта
- [ ] Добавлена документация для новых функций
- [ ] Обновлены файлы локализации (если нужно)

### 6. Pull Request

```bash
# Отправьте изменения в свой форк
git push origin feature/amazing-feature
```

Затем на GitHub:

1. Создайте Pull Request из вашей ветки в `master`
2. Заполните шаблон PR
3. Добавьте описание изменений
4. Привяжите связанные issues (если есть)
5. Дождитесь ревью

## Стандарты кода

### C++ Style Guide

```cpp
// Используйте snake_case для переменных и функций
int timer_duration;
void update_timer_state();

// Используйте PascalCase для классов и структур
struct TimerSource {
    int duration;
    bool is_running;
};

// Константы - UPPER_SNAKE_CASE
const int MAX_DURATION = 7200;

// Отступы - табы (как в OBS Studio)
void example_function()
{
	if (condition) {
		do_something();
	}
}

// Скобки на новой строке для функций
void function_name(int param)
{
	// код
}

// Скобки на той же строке для if/while/for
if (condition) {
	// код
} else {
	// код
}
```

### Комментарии

```cpp
// Однострочные комментарии для кратких пояснений

/* Многострочные комментарии
 * для более подробных описаний
 * или блоков кода
 */

/**
 * Документирующие комментарии для функций
 * @param source - источник OBS
 * @return true если успешно
 */
bool timer_start(obs_source_t *source);
```

### Память

```cpp
// Используйте функции OBS для управления памятью
char *text = bstrdup("hello");
bfree(text);

// Не используйте malloc/free напрямую
// Не используйте new/delete
```

## Структура проекта

```
Timer/
├── src/
│   └── timer-plugin.cpp       # Основной код (будет разделён в будущем)
├── data/
│   └── locale/                # Переводы
│       ├── en-US.ini
│       └── ru-RU.ini
├── .vscode/                   # Конфигурация VS Code
├── CMakeLists.txt             # Конфигурация сборки
└── docs/                      # Документация
    ├── README.md
    ├── INSTALL.md
    ├── EXAMPLES.md
    └── TODO.md
```

## Локализация

### Добавление нового языка

1. Создайте файл `data/locale/xx-XX.ini`
2. Скопируйте содержимое `en-US.ini`
3. Переведите все строки
4. Обновите `CMakeLists.txt` (если нужно)
5. Добавьте язык в `README.md`

Пример `fr-FR.ini`:
```ini
TimerSource="Minuteur à rebours"
Duration="Durée (secondes)"
Font="Nom de la police"
...
```

## Процесс ревью

После создания PR:

1. **Автоматические проверки** (когда будут настроены)
   - Сборка на всех платформах
   - Проверка стиля кода
   - Тесты

2. **Ревью кода**
   - Поддержка может запросить изменения
   - Обсуждение решений
   - Улучшение реализации

3. **Финальная проверка**
   - Тестирование в OBS
   - Проверка документации
   - Ребейс если нужно

4. **Мёрж**
   - PR будет смёржен в master
   - Изменения попадут в следующий релиз

## Вопросы?

- 💬 Создайте [Discussion](https://github.com/yourusername/obs-timer-plugin/discussions)
- 🐛 Создайте [Issue](https://github.com/yourusername/obs-timer-plugin/issues)
- 📧 Напишите мейнтейнерам

## Кодекс поведения

- Будьте уважительны к другим участникам
- Конструктивная критика приветствуется
- Помогайте новичкам
- Фокусируйтесь на коде, а не на личностях

## Лицензия

Внося вклад в проект, вы соглашаетесь с тем, что ваш код будет распространяться под лицензией MIT.

---

Спасибо за ваш вклад! 🚀
