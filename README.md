# OBS Timer Plugin - Плагин таймера обратного отсчёта для OBS Studio

Плагин для OBS Studio, который добавляет источник с таймером обратного отсчёта с настраиваемыми параметрами отображения и действиями по завершении.

## 🚀 Быстрая навигация

- 🎯 **Хочу использовать плагин** → [QUICKSTART.md](QUICKSTART.md)
- 🛠️ **Хочу собрать из исходников** → [QUICK_SETUP.md](QUICK_SETUP.md)
- 📦 **Установка зависимостей** → [DEPENDENCIES.md](DEPENDENCIES.md)
- 💾 **Установка готового плагина** → [INSTALL.md](INSTALL.md)
- 🎨 **Примеры настроек** → [EXAMPLES.md](EXAMPLES.md)
- ❓ **Текущий статус проекта** → [STATUS.md](STATUS.md)

## Возможности

- ⏱️ **Таймер обратного отсчёта** - настраиваемая длительность от 1 секунды до 2 часов
- 🎨 **Настройка внешнего вида** - выбор шрифта, размера и цвета текста
- ✨ **Эффекты появления и затухания** - плавное появление в начале и затухание в конце
- 🎯 **Действия по завершении**:
  - Ничего не делать
  - Скрыть источник
  - Сбросить и перезапустить
  - Показать пользовательское сообщение
- 🌍 **Мультиязычность** - поддержка русского и английского языков

## Требования

- OBS Studio 28.0 или новее
- CMake 3.16 или новее
- Компилятор C++17 (Visual Studio 2019+ для Windows, GCC 9+ для Linux, Clang для macOS)

## Сборка

### Windows

1. Установите Visual Studio 2019 или новее с поддержкой C++
2. Установите CMake (https://cmake.org/)
3. Клонируйте репозиторий OBS Studio или укажите путь к установленной версии

```bash
# Создайте директорию для сборки
mkdir build
cd build

# Сконфигурируйте проект
cmake .. -DCMAKE_PREFIX_PATH="C:/Path/To/OBS/cmake"

# Соберите плагин
cmake --build . --config Release
```

4. Скопируйте скомпилированный файл `obs-timer-plugin.dll` в:
   ```
   C:\Program Files\obs-studio\obs-plugins\64bit\
   ```

5. Скопируйте папку `data/locale` в:
   ```
   C:\Program Files\obs-studio\data\obs-plugins\obs-timer-plugin\
   ```

### Linux

```bash
mkdir build && cd build
cmake .. -DCMAKE_PREFIX_PATH="/usr/lib/cmake"
make
sudo make install
```

### macOS

```bash
mkdir build && cd build
cmake .. -DCMAKE_PREFIX_PATH="/Applications/OBS.app/Contents/Resources/cmake"
make
sudo make install
```

## Использование

1. Запустите OBS Studio
2. В списке источников нажмите "+" (Добавить)
3. Выберите "Countdown Timer" (Счётчик обратного отсчёта)
4. Настройте параметры таймера

### Настройки

#### Основные параметры
- **Duration (Длительность)** - время обратного отсчёта в секундах (1-7200)
- **Font Name (Название шрифта)** - название системного шрифта (например, "Arial", "Times New Roman")
- **Font Size (Размер шрифта)** - размер текста в пикселях (12-200)
- **Text Color (Цвет текста)** - цвет отображаемого текста

#### Эффекты
- **Fade In Duration (Длительность появления)** - время плавного появления в начале (0-10 секунд)
- **Fade Out Duration (Длительность затухания)** - время плавного затухания в конце (0-10 секунд)

#### Действия
- **Action When Finished (Действие по завершении)**:
  - **None (Нет действия)** - таймер просто остановится на 00:00
  - **Hide Source (Скрыть источник)** - источник автоматически скроется
  - **Reset and Restart (Сбросить и перезапустить)** - таймер автоматически начнёт новый отсчёт
  - **Show Message (Показать сообщение)** - вместо времени будет показано указанное сообщение

- **Finish Message (Сообщение по завершении)** - текст, который отображается при выборе действия "Show Message"

#### Управление
- **Start Timer (Запустить таймер)** - начать отсчёт
- **Pause Timer (Приостановить таймер)** - приостановить отсчёт
- **Reset Timer (Сбросить таймер)** - сбросить на начальное значение

## Примеры использования

### Таймер для стримов
Установите таймер на 10 минут (600 секунд) с действием "Hide Source" для автоматического скрытия по окончании перерыва.

### Обратный отсчёт до события
Установите таймер с пользовательским сообщением "START!" и действием "Show Message" для эффектного начала трансляции.

### Циклический таймер
Используйте действие "Reset and Restart" для создания повторяющегося таймера.

## Известные ограничения

- Текст отображается в формате MM:SS (минуты:секунды)
- Максимальная длительность - 2 часа (7200 секунд)
- Рендеринг текста использует упрощённую реализацию (в будущих версиях планируется улучшение)

## Разработка

### Структура проекта

```
Timer/
├── src/
│   └── timer-plugin.cpp      # Основной код плагина
├── data/
│   └── locale/
│       ├── en-US.ini          # Английская локализация
│       └── ru-RU.ini          # Русская локализация
├── CMakeLists.txt             # Конфигурация сборки
└── README.md                  # Документация
```

### Основные компоненты

- `timer_source` - структура данных источника
- `timer_tick()` - обновление состояния таймера
- `timer_render()` - отрисовка таймера
- `timer_video_tick()` - обновление текстуры с текстом
- `timer_properties()` - определение настроек в UI

## Лицензия

Этот проект распространяется под лицензией MIT. Вы можете свободно использовать, модифицировать и распространять его.

## Поддержка

Если у вас возникли проблемы или есть предложения по улучшению, создайте issue в репозитории проекта.

## Авторы

OBS Timer Plugin - плагин для OBS Studio

## Благодарности

- Команде OBS Studio за отличную платформу для стриминга
- Сообществу разработчиков плагинов OBS
