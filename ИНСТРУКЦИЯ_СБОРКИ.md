# 🎯 ГОТОВЫЙ ПЛАГИН - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ✨ Самый простой способ получить готовый .dll плагин

### Вариант 1: GitHub Actions (10 минут) - РЕКОМЕНДУЮ ⭐

#### Шаг 1: Подготовьте проект

Откройте **новый** PowerShell и выполните:

```powershell
# Перейдите в директорию проекта
cd d:\OBS_Plugins\Timer

# Инициализируйте Git
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "OBS Timer Plugin v1.0.0"
```

#### Шаг 2: Создайте репозиторий на GitHub

1. Откройте https://github.com/new в браузере
2. Заполните:
   - **Repository name**: `obs-timer-plugin`
   - **Description**: "OBS Studio Countdown Timer Plugin"
   - **Public** или **Private** - любой
   - **НЕ** добавляйте README, .gitignore, license (уже есть!)
3. Нажмите **"Create repository"**

#### Шаг 3: Отправьте код на GitHub

Скопируйте команды, которые GitHub покажет на следующей странице:

```powershell
# Замените YOUR_USERNAME на ваш никнейм GitHub
git remote add origin https://github.com/YOUR_USERNAME/obs-timer-plugin.git
git branch -M main
git push -u origin main
```

**Важно:** При первом push GitHub попросит авторизацию.

#### Шаг 4: Запустите сборку

1. Перейдите: `https://github.com/YOUR_USERNAME/obs-timer-plugin/actions`
2. Увидите предупреждение: **"Workflows aren't being run on this forked repository"**
3. Нажмите: **"I understand my workflows, go ahead and enable them"**
4. Слева выберите: **"Build OBS Timer Plugin"**
5. Справа нажмите: **"Run workflow"** (зеленая кнопка)
6. Еще раз: **"Run workflow"** (подтвердить)
7. Ждите 5-10 минут ⏳ (можете закрыть страницу)

#### Шаг 5: Скачайте готовый плагин

1. Обновите страницу через 10 минут
2. Увидите зеленую галочку ✅ возле сборки
3. Нажмите на название сборки (например: "Build OBS Timer Plugin")
4. Внизу страницы раздел **"Artifacts"**
5. Скачайте: **`obs-timer-plugin-windows`** (ZIP файл)
6. Распакуйте ZIP

Внутри будет:
```
obs-timer-plugin.dll      ← Готовый плагин!
data/
  locale/
    en-US.ini
    ru-RU.ini
```

#### Шаг 6: Установите в OBS

```powershell
# 1. Скопируйте DLL плагина
Copy-Item "obs-timer-plugin.dll" "C:\Program Files\obs-studio\obs-plugins\64bit\"

# 2. Скопируйте файлы локализации
$destDir = "C:\Program Files\obs-studio\data\obs-plugins\obs-timer-plugin"
if (!(Test-Path $destDir)) { New-Item -ItemType Directory -Path $destDir }
Copy-Item -Recurse "data\locale" "$destDir\"
```

Или вручную:
1. Файл `obs-timer-plugin.dll` → `C:\Program Files\obs-studio\obs-plugins\64bit\`
2. Папку `data\locale` → `C:\Program Files\obs-studio\data\obs-plugins\obs-timer-plugin\`

#### Шаг 7: Используйте!

1. Запустите **OBS Studio**
2. В панели Источники нажмите **➕**
3. Выберите **"Countdown Timer"** (или "Счётчик обратного отсчёта")
4. Настройте параметры
5. Готово! 🎉

---

### Вариант 2: Docker (если есть Docker Desktop)

```powershell
cd d:\OBS_Plugins\Timer
.\build_docker.bat
```

Создаст `release/obs-timer-plugin.so` (Linux версия)

---

### Вариант 3: Попросить готовую сборку

Если GitHub Actions не работает:

1. Создайте Issue: https://github.com/yourusername/obs-timer-plugin/issues/new
2. Напишите: "Не могу собрать плагин, нужна готовая версия для Windows x64"
3. Я или сообщество предоставим готовый .dll файл

---

## 🎓 Подробная справка

### Что если GitHub попросит логин?

**Windows PowerShell:**
- При `git push` откроется окно авторизации
- Введите login/password или используйте token
- Или установите GitHub CLI: `winget install GitHub.cli`

**Использование токена:**
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. Выберите scope: `repo`
4. Используйте token вместо пароля

### Что если сборка failed (красный крестик)?

1. Нажмите on failed сборку
2. Посмотрите логи
3. Скорее всего проблема с GitHub Actions конфигурацией
4. Создайте Issue с логом ошибки

### Нужен ли платный GitHub?

**Нет!** Бесплатный акк аунт дает:
- Unlimited public/private repos
- 2000 минут Actions/месяц (хватит на 200 сборок!)
- Все функции для Open Source

### Сколько места на диске?

- Проект: ~5 MB
- Клонированный OBS в C:\obs-studio: ~300 MB (можно удалить после)
- Готовый плагин: ~100 KB

### Можно удалить C:\obs-studio?

**Да!** После получения готового плагина можете удалить:
```powershell
Remove-Item -Recurse -Force "C:\obs-studio"
```

---

## ⚡ Быстрый чеклист

- [ ] Открыл PowerShell в `d:\OBS_Plugins\Timer`
- [ ] `git init` && `git add .` && `git commit`
- [ ] Создал репозиторий на GitHub
- [ ] `git remote add origin ...` && `git push`
- [ ] Enabled workflows на GitHub
- [ ] Запустил "Run workflow"
- [ ] Подождал 10 минут ☕
- [ ] Скачал Artifact с готовым .dll
- [ ] Установил в OBS
- [ ] Работает! 🎉

---

## 📞 Нужна помощь?

**Проблемы с Git:**
- https://git-scm.com/doc

**Проблемы с GitHub:**
- https://docs.github.com/en/actions

**Проблемы с плагином:**
- Создайте Issue с описанием проблемы
- Приложите скриншоты
- Укажите версию OBS Studio

---

## 🎁 Бонус: Автоматические обновления

После первой настройки:

1. **Изменили код?** 
   ```powershell
   git add .
   git commit -m "Update feature"
   git push
   ```

2. **Автоматически пересоберется!**
   - Проверьте Actions через 10 минут
   - Скачайте новую версию из Artifacts

3. **Всегда свежая версия** без ручной сборки!

---

**Успехов! 🚀**

*Если что-то непонятно - создайте Issue, помогу разобраться!*
